name: speed_alert_pro
description: Cross-platform Speed Alert Pro — iOS and Android (Flutter).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  geolocator: ^13.0.1
  permission_handler: ^11.3.1
  shared_preferences: ^2.3.2
  http: ^1.2.2
  supabase_flutter: ^2.8.0
  purchases_flutter: ^8.10.2
  google_sign_in: ^6.2.2
  google_maps_flutter: ^2.9.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
