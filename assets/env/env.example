# Copy this file to `.env` in the same folder and fill in values.
# `assets/env/.env` is gitignored — never commit real keys.
#
# Precedence: `--dart-define=KEY=value` (non-empty) > `.env` > empty placeholders below.

SUPABASE_URL=
SUPABASE_ANON_KEY=
REVENUECAT_PUBLIC_API_KEY=
HERE_API_KEY=
GOOGLE_MAPS_API_KEY=
GOOGLE_WEB_CLIENT_ID=
MAPBOX_ACCESS_TOKEN=
TOMTOM_API_KEY=
